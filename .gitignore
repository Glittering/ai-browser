node_modules/
dist/
.env
*.log
.DS_Store
Electron.app/
cache/
screenshots/
# tools/ holds a WS helper the committed e2e/ suite depends on (../tools/browser.cjs);
# archive it so CI/repros don't break, keep the one real asset tracked.
tools/**
!tools/browser.cjs
.exec-trail/